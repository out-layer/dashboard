// Canonical site origin — the ONE place to flip when app.outlayer.ai becomes
// canonical. Used by root metadata (metadataBase), sitemap and robots.
export const SITE_ORIGIN = 'https://outlayer.fastnear.com';
